# Where the wall reads projects from (the FastAPI submissions server).
# On ascii.dev the wall is served same-origin, so leave this empty and it will
# fetch relative paths like /submissions. Point it elsewhere for local dev.
VITE_API_BASE=http://localhost:8000
